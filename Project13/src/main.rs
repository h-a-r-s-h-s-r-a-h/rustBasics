fn main() {
    let tuple1 = (1, 2);
    let tuple2 = (1, 3);

    println!("Are they equal? {}", tuple1 == tuple2); // Outputs false
    println!("Is tuple1 less than tuple2? {}", tuple1 < tuple2); // Outputs true
}
